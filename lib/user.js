// import { User } from './models';  // Assuming you have a User model defined with Sequelize

export async function getUserByEmail(email) {
    return {email: 'lqDmH@example.com', password_hash: '12345678', id: "ssss"}
  // return await User.findOne({ where: { email } });
}
